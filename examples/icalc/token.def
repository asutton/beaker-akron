// Copyright (c) 2015-2017 Andrew Sutton
// All rights reserved

def_token(eof)        // <eof>
def_token(lparen)     // (
def_token(rparen)     // )
def_token(plus)       // +
def_token(minus)      // -
def_token(star)       // *
def_token(slash)      // /
def_token(percent)    // %
def_token(amp)        // &
def_token(bar)        // |
def_token(caret)      // ^
def_token(tilde)      // ~
def_token(eq_eq)      // ==
def_token(bang_eq)    // !=
def_token(lt)         // <
def_token(gt)         // >
def_token(lt_eq)      // <=
def_token(gt_eq)      // >=
def_token(amp_amp)    // &&
def_token(bar_bar)    // ||
def_token(bang)       // !
def_token(question)   // ?
def_token(colon)      // :
def_token(bool)       // true | false
def_token(int)        // digit digit*

#undef def_token
